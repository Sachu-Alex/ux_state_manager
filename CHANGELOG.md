## 0.1.2

* Added full dartdoc comments to all public API elements

## 0.1.1

* Re-release with corrected package metadata

## 0.1.0

* Initial release
* `UxStateBuilder` widget with `AnimatedSwitcher` transitions
* `UxController` — `ChangeNotifier` with `execute`, `retry`, and `reset`
* `UxState<T>` — generic immutable state with factory constructors
* `RetryStrategy` — configurable auto-retry with optional delay
* Built-in `LoadingView`, `ErrorView`, and `EmptyView` default widgets
* Smart empty detection for `List`, `Map`, and `String` results
